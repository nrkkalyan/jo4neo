#summary data types supported by jo4neo

= Introduction =

jo4neo maps java objects to nodes and relations, however, it doesn't support all java types.

= Details =

|| type || persistence support || indexing support || fluent api support ||
|| int, long, float, double  || yes                 || yes              || 
|| Integer, Long, Float, Double || yes             || yes               ||